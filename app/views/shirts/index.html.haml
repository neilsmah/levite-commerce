#shirts.transitions-enabled
	- @shirts.each do |shirt|
		.box.panel.panel-default
			= link_to (image_tag shirt.image.url(:medium)), shirt
			.panel-body
				%h2= link_to shirt.name, shirt
				%p= shirt.price


